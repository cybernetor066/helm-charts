apiVersion: v3
kind: Pod
metadata:
  name: "{{ include "test_app01.fullname" . }}-test-connection"
  labels:
    {{- include "test_app01.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test_app01.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
